# Copy to functions/.env. These are plain settings, not secrets.
# Firebase loads this file into process.env for both the emulators and deploys.
MAIL_FROM="Azteca del Golfo <noreply@aztecadelgolfo.com>"
BOARD_EMAIL=board@aztecadelgolfo.com
SITE_URL=https://aztecadelgolfofishingclu-bc814.web.app

# The Resend API key is a secret and does NOT go in this file:
#   firebase functions:secrets:set RESEND_API_KEY
# Without it the functions run normally and log the email instead of sending it.
